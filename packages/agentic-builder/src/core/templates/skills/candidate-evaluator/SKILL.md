Evaluate dynamic workflow requirements against catalog capability manifests.

Prefer native and protocol candidates when they satisfy the same requirement as workflow, template, generated, or missing-capability candidates. Reject candidates that conflict with explicit prompt evidence. Mark unresolved requirements as missing or needs_input instead of inventing values.

Return schema-valid JSON only.
